# Аудит проекта "Код Мастер"

Дата аудита: 12.07.2026

## 1. Главное окно (ui/main_window.py)

- ✅ В верхней панели добавлена кнопка «Тема» с выпадающим меню (Тёмный / Светлый).
- ✅ Убраны кнопки «DBC» и «Выход» из верхней панели.
- ✅ Кнопки «Обновить» и «Настроить» перемещены в тело окна, увеличены до 240×100px и расположены в правом верхнем углу центральной области.
- ✅ При нажатии «Настроить» главное окно скрывается и открывается `SettingsWindow`.
- ✅ В `SettingsWindow` добавлена кнопка «Назад», которая закрывает окно настроек и возвращает главное окно.

## 2. Страница прошивки (ui/firmware_page.py)

- ✅ Интерфейс содержит три столбца: «ПО блока», «Автомобиль», «Конфигурация».
- ✅ Столбец «ПО блока»: `QListWidget` с отсортированным демо-списком версий.
- ✅ Столбец «Автомобиль»: `QLineEdit` поиска, `QListWidget` с демо-списком авто, фильтрация работает.
- ✅ В каждом столбце присутствуют кнопки «Обновить» и 📂.
- ✅ Кнопки 📂 открывают `QFileDialog` и запускают `BootloaderWorker` с выбранным файлом.
- ✅ Демо-элементы отображают пометку «(загрузите файл)»; кнопка «Обновить» требует предварительного выбора файла и выводит предупреждение.

## 3. Триггеры (ui/can_trigger_tab.py)

- ✅ 10 независимых блоков с одинаковым макетом.
- ✅ В каждом блоке: чекбокс «Активен», строка «Приём» (канал, битность ID, ID с HEX-валидацией, DLC 1–8, 8 полей Data).
- ✅ HEX-поля автоматически приводятся к верхнему регистру; ID подсвечивается зелёным/красным по допустимому диапазону.
- ✅ Доступность полей Data зависит от значения DLC.
- ✅ Блок «Ответ» — единый динамический список фреймов: кнопки «+»/«–» для добавления/удаления строки; каждая строка содержит канал, битность, ID, DLC, 8 Data, «Задержка мс» (0–9999).
- ✅ Отправка фреймов ответа последовательная с накапливаемыми задержками.
- ✅ Секция «Автоматическая запись DATA в Кэш» с чекбоксом «Активно»: при включении ответы блокируются, кэш разблокируется.
- ✅ Строка кэша: битность, ID, DLC, «От» + 8 Data, «До» + 8 Data.
- ✅ Логика кэша: сохраняется последний кадр, ID и DATA которого попадают в диапазон [От, До] (big-endian); при срабатывании триггера отправляется сохранённый кадр.
- ✅ Убран счётчик срабатываний.

## 4. Мониторинг (ui/can_monitor_tab.py)

- ✅ Убраны кнопки «Загрузить DBC» и «Экспорт», чекбокс «Только известные ID», переключатель «Поток / Уникальные ID».
- ✅ Вместо переключателя режимов добавлена кнопка «Фильтр» с заглушкой «Функция в разработке».
- ✅ Таблицы CAN1 и CAN2 имеют 7 колонок: ID, DLC, DATA, Период, Счётчик, ASCII, Пояснение.
- ✅ Панель отправки: выбор битности, ID с валидацией, DLC 1–8, 8 Data, «Период мс» (0–9999), кнопка «Отправить» и кнопка «∞» с фиксацией для циклической отправки.
- ✅ Убраны кнопка «Из DBC» и история пакетов.

## 5. Вкладка «Трэйс»

- ✅ В `ui/settings_window.py` вкладка `CanAnalyzer` добавлена с заголовком `tr("Трэйс")`.
- ✅ Перевод «Трэйс» → "Trace" присутствует в `models/translations.py`.
- ✅ В `ui/can_analyzer.py` заголовок, docstring и сообщения лога приведены к термину «Трэйс CAN-шины».

## 6. Нижние кнопки в окне настроек

- ✅ Осталось три кнопки: «Сохранить», «Сохранить конфигурацию», «Заводские настройки».
- ✅ «Сохранить» вызывает `Config.save()` и сохраняет триггеры/правила.
- ✅ «Сохранить конфигурацию» экспортирует JSON через `QFileDialog`.
- ✅ «Заводские настройки» сбрасывает конфиг через `Config.reset_to_defaults()` с подтверждением `QMessageBox`.

## 7. CAN-шлюз (ui/can_gateway_tab.py)

- ✅ Вкладка реализована полноценно.
- ✅ Секция «Игнорировать»: 10 полей ID, чекбокс «Включить игнорирование».
- ✅ Секция «Правила подмены»: 10 блоков с приёмом/подменой ID+Data, направлением и чекбоксом.
- ✅ Кнопки «Запустить шлюз» / «Остановить», сохранение/загрузка JSON.
- ✅ Логика ретрансляции, игнорирования по ID и подмены по правилам реализована в `process_frame`.

## 8. Гибкая логика (ui/flexible_logic_tab.py)

- ✅ В диалог редактирования правила добавлена кнопка «Из DBC».
- ✅ При выборе сигнала автоматически заполняются ID условия и эталонные данные.
- ✅ Маска данных устанавливается в `FF ` × 8.
- ✅ `DbcSignalDialog` использует `DBCManager`, поэтому список сигналов актуален при смене DBC.

## 9. Сборочные файлы

- ✅ `build_win.spec`, `build_mac.spec`, `build_linux.spec` содержат `hiddenimports` для `PySide6`, `matplotlib`, `pyqtgraph`, `cantools`, `serial`, `logging.handlers` и всех внутренних модулей.
- ✅ `.github/workflows/build.yml` содержит три job'а (Windows, macOS, Linux), создание `.dmg` через `hdiutil`, загрузку артефактов и релиз по тегу `v*` и `latest` pre-release при пуше в `main`.
- ✅ `build_mac.spec` формирует `.app` через `BUNDLE`.

## 10. Запуск и стабильность

- ✅ `python3 -m py_compile` для всех изменённых файлов проходит без ошибок.
- ✅ Приложение запускается, главное окно и окно настроек открываются, переключение между вкладками работает.
- ✅ После финальных правок не обнаружено критических регрессий.

## 11. Генеральная уборка UI (12.07.2026)

- ✅ Блок «Ответ» в триггерах переработан в два ряда: сверху канал, битность, ID, DLC, 8 Data; снизу — задержка мс, количество отправок, кнопка «Удалить фрейм».
- ✅ Кнопки «+»/«–» заменены на текстовые «Добавить фрейм» / «Удалить фрейм» с корректной видимостью в тёмной теме.
- ✅ Между фреймами ответа добавлены разделительные линии.
- ✅ Добавлено поле «Кол-во отправок» (1–64) для каждого фрейма ответа; логика отправки учитывает счётчик.
- ✅ Во всех вкладках кнопки получили политику `MinimumExpanding` и авторазмер по содержимому.
- ✅ В `SettingsWindow` добавлен навигационный поиск по разделам с переключением на первую подходящую вкладку.
- ✅ CAN-шлюз и мониторинг: увеличены минимальные ширины полей ID и Data, панель отправки разбита на два ряда.
- ✅ Все новые строки UI обёрнуты в `tr()`.

## 12. Финальные правки интерфейса и логики триггеров (12.07.2026)

- ✅ Блок «Кэш» в триггерах дополнен выбором канала отправки, задержкой (0–9999 мс) и количеством повторных отправок (1–999).
- ✅ Блок «Ответ» ограничен 5 фреймами; между фреймами добавлено поле «Пауза перед следующим».
- ✅ Строка фрейма ответа размещена в одном ряду: канал, битность, ID, DLC, Data, задержка, количество, кнопка «–».
- ✅ Кнопки «+»/«–» в блоке «Ответ» уменьшены до 30×30 px с фоном #3A3A5A и белым текстом.
- ✅ Триггерные блоки обёрнуты в сворачиваемые `QGroupBox` с `setCheckable(True)`.
- ✅ DLC `QSpinBox` получил минимальную ширину 50 px, высоту 24 px, шрифт 11 pt и белый текст на фоне #2B2B3C.
- ✅ Глобальный стиль `QCheckBox` в тёмной теме: квадратный индикатор с рамкой #6C8CFF, белая галочка, фон #2B2B3C.
- ✅ Увеличена ширина окна настроек до 1100 px для размещения расширенных строк фреймов.

## 13. Исправление падения macOS-приложения на старте (12.07.2026)

- ✅ Диагностировано: на старте `setup_logging()` пытался создать папку `logs` внутри read-only `.app` бандла, что приводило к исключению до создания `QApplication`.
- ✅ `sys.excepthook` (`show_exception_box`) в `ui/main_window.py` теперь не вызывает `QMessageBox`, если `QApplication` ещё не создан, и пишет ошибку в `stderr`.
- ✅ `models/logger.py` переключено на `platformdirs.user_log_dir` — логи теперь пишутся в `~/Library/Logs/CodeMaster` (macOS), `%LOCALAPPDATA%\CodeMaster\Logs` (Windows) и т. д.
- ✅ `models/config.py` переключено на `platformdirs.user_data_dir` — `config.json` теперь хранится в `~/Library/Application Support/CodeMaster`.
- ✅ Пути `library`/`dbc`/`scripts` переключены на `get_library_root()` в `models/utils.py`, которая возвращает доступное для записи пользовательское место и копирует bundled defaults при первом запуске.
- ✅ `build_mac.spec` теперь включает `library` в `datas` для копирования в `.app`.
- ✅ `platformdirs>=4.0` добавлен в `requirements.txt`.
- ✅ `python3 -m py_compile` и запуск из read-only копии прошли успешно.
- ✅ Диагностировано зависание на Windows при старте эмуляции в мониторе: `CanMonitorTab.add_frame` на каждый кадр применяла фильтры и поиск по всем строкам таблицы (O(n) на фрейм).
- ✅ `CanMonitorTab.add_frame` больше не вызывает `_apply_filters` / `_apply_search` при получении кадра; фильтры и поиск применяются только при изменении соответствующих полей.
- ✅ Добавлены `textChanged` соединения для полей фильтрации (`ID от`, `ID до`, `Искл.`).

## 14. Исправление зависания при открытии настроек с эмулятором (12.07.2026)

- ✅ [x] Исправлено зависание при запуске эмулятора.
- ✅ Диагностировано: `CanTriggerTab._set_response_rows` при пустом списке `responses` зацикливался на бесконечность, пытаясь удалить последний защищённый ряд. Главный поток блокировался при создании `SettingsWindow` после подключения эмулятора.
- ✅ Условие цикла в `_set_response_rows` исправлено: удаление прекращается, когда остаётся минимум 1 ряд (`ui/can_trigger_tab.py`).
- ✅ Проверены `SerialManager.open_port`, `FakeSerial.__init__`/`open`, `ComSettingsDialog`, `main_window.py` — блокирующих вызовов и циклов в главном потоке не обнаружено.
- ✅ `FakeSerial` больше не использует `pack_can_frame` + `_swap_marker`, который портил XOR-контрольную сумму; вместо этого формируется входящий кадр с маркером RX и правильной чексуммой (`core/fake_serial.py`).
- ✅ Локальный smoke-test: `MainWindow` → «Настроить» → `SettingsWindow` открывается без зависаний, эмуляция отправляет CAN-кадры в оба монитора.

## 15. Финальные правки UI и функционала перед релизом (13.07.2026)

- ✅ `ui/can_trigger_tab.py`: длина полей ID ограничена динамически — 3 HEX для 11 бит, 8 HEX для 29 бит; `_IdValidator` хранится на виджете, чтобы слот не удалялся сборщиком мусора.
- ✅ `ui/can_trigger_tab.py`: чекбокс «Автоматическая запись DATA в Кэш» перенесён внутрь группы «Кэш» перед полями; отдельный чекбокс «Активен» удалён. Активность триггера управляется галочкой самой группы. При установленной галочке доступны поля кэша и блокируется блок ответа, при снятой — наоборот.
- ✅ `ui/can_trigger_tab.py`: кнопки «Добавить фрейм» (+) и «Удалить фрейм» (−) в блоке ответа оформлены в виде иконочных кнопок 26×26 px, жирный 14pt белый текст, фон #3A3A5A, hover #4A4A6A.
- ✅ `ui/can_trigger_tab.py`, `ui/can_monitor_tab.py`: поля Data (HEX) автоматически переводят фокус на следующее поле после ввода второго допустимого HEX-символа; ввод приводится к верхнему регистру.
- ✅ `ui/main_window.py`: кнопка «Прошить\nмикроконтроллер» увеличена до 260×110 px, текст перенесён на две строки, шрифт 13pt; надпись полностью читается в тёмной теме.
- ✅ `ui/main_window.py`, `ui/settings_window.py`, основные вкладки: реализован `retranslate_ui()` — смена языка в главном окне мгновенно переводит открытые окна.
- ✅ `ui/can_monitor_tab.py`: кнопки «Старт»/«Стоп»/«Очистить» и глобальные кнопки мониторинга 80×28 px, шрифт 9pt; кнопка циклической отправки «∞» 36×36 px, Arial 20pt bold.
- ✅ `ui/firmware_page.py`: кнопки выбора файла заменены на чёткую стандартную иконку `SP_DirOpenIcon` (36×36 px, иконка 24×24 px) с hover-эффектом.
- ✅ `python3 -m py_compile` проходит; smoke-test: главное окно, окно настроек, триггеры, переключение языка, мониторинг и диалог прошивки запускаются без ошибок.

## 16. Финальная полировка UX и функционала (13.07.2026)

- ✅ `ui/hex_edit.py`, `ui/can_trigger_tab.py`, `ui/can_monitor_tab.py`: создан единый виджет `HexDataEdit` для полей Data (HEX). Автопереход фокуса на следующее поле после ввода 2-го HEX-символа, переход на предыдущее поле по Backspace в пустом поле, очистка и блокировка полей при уменьшении DLC.
- ✅ `ui/can_trigger_tab.py`: кнопки «Добавить фрейм» (+) и «Удалить фрейм» (−) увеличены до 32×32 px, шрифт 16pt bold, фон #4A4A6A, hover #5A5A7A.
- ✅ `ui/can_trigger_tab.py`: удалены нижние кнопки «Применить/Сохранить/Загрузить триггеры». Триггеры активируются автоматически по состоянию групп.
- ✅ `ui/can_monitor_tab.py`: удалены локальные кнопки «Старт/Стоп/Очистить» и чекбокс «Приостановить» для каждого канала; мониторинг запускается автоматически. Оставлены глобальные кнопки «Запустить/Остановить/Очистить всё».
- ✅ `ui/can_monitor_tab.py`: увеличены элементы панели отправки: поле «Период мс» min-width 90px, кнопка «Отправить» min-width 100px, кнопка «∞» 44×36 px Arial 20pt bold.
- ✅ `ui/can_monitor_tab.py`: таблицы мониторинга сортируются по ID по убыванию; при изменении данных для существующего ID подсветка ячейки DATA красным (#FF4444) на 500 мс с учётом интервала фильтра.
- ✅ `ui/filter_dialog.py`, `ui/can_monitor_tab.py`: добавлен модальный диалог фильтра с правилами (ID + диапазон Data), чекбоксом «Включить фильтр» и настройкой интервала подсветки.
- ✅ `ui/can_analyzer.py`: вкладка «Трэйс» переработана — две таблицы CAN1/CAN2 с колонками Время, ID, DLC, DATA, Период, ASCII, Пояснение; кнопки «Начать/Завершить анализ»; правый клик «Копировать пакет» в формате `ID=<hex> DLC=<n> DATA=<...>`.
- ✅ `models/utils.py`, `ui/id_edit.py`, `ui/can_monitor_tab.py`, `ui/can_trigger_tab.py`: при вставке строки пакета в поле ID поля ID, DLC и Data заполняются автоматически.
- ✅ `python3 -m py_compile` проходит; smoke-test расширен и проверяет автопереход Data, вставку пакета, фильтр/сортировку/подсветку в Мониторинге и запись Трейса.

## 17. Поддержка разных устройств (CAN 2.0, CAN FD, аналоговые порты) (13.07.2026)

- ✅ `core/can_protocol.py`: добавлены команды `CMD_DEVICE_ID = 0x90`/`CMD_DEVICE_ID_RESP = 0x91` и `CMD_AUTO_SPEED = 0xA0`/`CMD_AUTO_SPEED_RESP = 0xA1`; увеличен лимит `pack_can_frame` до 64 байт для CAN FD.
- ✅ `core/fake_serial.py`: эмулятор отвечает на запрос ID как CAN FD (type=0x01) и на автоскорость возвращает 500 кбит/с.
- ✅ `models/config.py`: добавлены поля `device_type`, `device_version`, `can1_speed`, `can2_speed`, `can_speed_auto`; метод `max_data_bytes()` возвращает 64 для CAN FD и 8 для остальных.
- ✅ `core/serial_manager.py`: при открытии порта отправляется `CMD_DEVICE_ID`, результат сохраняется в Config; добавлен метод `auto_detect_can_speed()` с таймаутом 3 с и сигналы `device_identified`/`can_speed_detected`.
- ✅ `ui/com_settings_dialog.py`: добавлены раздельные комбобоксы скоростей CAN1/CAN2, кнопка «Автоопределение скорости» в фоновом потоке, отображение типа устройства; поля объединяются для базового CAN 2.0.
- ✅ `ui/hex_edit.py`: добавлен `create_data_field_widget()` — горизонтальный контейнер с `HexDataEdit`, для count > 8 оборачивается в `QScrollArea`.
- ✅ `ui/can_trigger_tab.py`, `ui/can_monitor_tab.py`: поля Data адаптируются к `max_data_bytes` (8/64); при смене устройства через `device_identified` поля пересоздаются с сохранением текущих значений.
- ✅ `ui/can_monitor_tab.py`: для CAN FD добавлены заглушки-чекбоксы FD, BRS, ESI.
- ✅ `ui/analog_ports_tab.py`: новая вкладка-заглушка с таблицей настройки имён/цветов пинов для устройств с аналоговыми портами.
- ✅ `ui/settings_window.py`: вкладка «Аналоговые порты» добавляется/удаляется автоматически при смене типа устройства.
- ✅ `python3 -m py_compile` проходит; smoke-test проверяет запуск интерфейса с новыми полями и динамическую адаптацию под CAN FD.

## 18. Финализация: удаление CAN FD и UI-полировка (14.07.2026)

- ✅ `core/can_protocol.py`: `pack_can_frame` ограничен 8 байтами (CAN 2.0); удалены константы и упоминания CAN FD.
- ✅ `models/config.py`: `max_data_bytes()` всегда возвращает 8; удалены CAN FD специфичные поля.
- ✅ `ui/can_trigger_tab.py`, `ui/can_monitor_tab.py`: удалены чекбоксы FD/BRS/ESI, динамическое пересоздание Data отключено, поля Data фиксированы 8 байт.
- ✅ `core/fake_serial.py`: эмулятор возвращает базовый CAN 2.0 `DEVICE_TYPE_BASIC` с `serial_number` и `total_memory`.
- ✅ `core/serial_manager.py`: парсится `serial_number` и `total_memory` из ответа устройства, сохраняется в `Config`, эмитятся сигналы.
- ✅ `ui/com_settings_dialog.py`: убраны CAN FD упоминания, добавлен ручной ввод CAN1/CAN2 скоростей, отображается серийный номер и объём памяти.
- ✅ `ui/can_trigger_tab.py`: реализована визуальная бледность неактивных блоков «Ответ» и «Кэш».
- ✅ `ui/dark_theme.py`: `QCheckBox::indicator:checked` использует SVG-галочку `assets/icons/check.svg`.
- ✅ `ui/can_monitor_tab.py`: удалены глобальные кнопки Start/Stop/Clear; добавлены per-канальные кнопки Запустить/Остановить/Очистить; кнопка «Фильтр» и QSpinBox интервала подсветки перенесены в глобальный уровень.
- ✅ `ui/filter_dialog.py`: переработан модальный диалог с правилами «Показывать/Не показывать» (ID + диапазон Data), вкладкой «Принятые» с динамически обновляемым списком ID и чекбоксами игнорирования; интервал подсветки вынесен в основное окно.
- ✅ `ui/memory_indicator.py`: добавлен индикатор использования памяти в `CanTriggerTab`, `CanMonitorTab`, `CanGatewayTab`.
- ✅ `ui/settings_window.py`: поиск по разделам реализован через `QCompleter` с выпадающим списком и расширенным словарём ключевых слов.
- ✅ `ui/can_analyzer.py`: добавлены кнопки экспорта CSV и `.trace`; разделены таблицы CAN1/CAN2; добавлены контекстные пункты «Копировать для отправки/триггера»; пояснения от DBC выводятся в таблицу.
- ✅ `ui/library_browser.py`: переработана в CAN ID библиотеку с деревом «Производитель → Модель → Год», загрузкой DBC из `library/dbc`, демо-данными, добавлением пользовательских авто/ID и связью с триггером.
- ✅ `python3 -m py_compile` проходит; smoke-test: главное окно, окно настроек, библиотека, мониторинг, триггеры, трейс запускаются без ошибок.

## 20. Юридическое предупреждение при запуске (14.07.2026)

- ✅ `ui/disclaimer_dialog.py`: модальное окно `QDialog` с заголовком `tr("ВНИМАНИЕ!")`.
- ✅ Текст отказа от ответственности обёрнут в `tr()` и выводится в `QLabel` с `wordWrap`.
- ✅ Кнопки `tr("Принимаю")` (Accept) и `tr("Выход")` (Reject); `Выход` завершает приложение.
- ✅ Окно стилизовано в тёмной теме: фон `#252538`, белый текст, оранжевый заголовок `#FF9800`, чёрные пилюлевидные кнопки с hover `#FF9800`.
- ✅ `main.py` показывает `DisclaimerDialog` сразу после `apply_dark_theme` и до создания `MainWindow`; диалог показывается при каждом запуске, состояние не сохраняется.

## 19. Вкладка «Логика» и автономная библиотека CAN ID (14.07.2026)

- ✅ `ui/logic_tab.py`: новая вкладка «Логика» с тремя блоками «События», «Условия», «Действия».
- ✅ В каждом блоке — динамический список строк, кнопка «Добавить» и крестик удаления.
- ✅ В строке выбор типа «Вход/выход» (именованный канал) или «CAN-сообщение».
- ✅ При выборе «CAN-сообщение» строка расширяется: битность ID, HEX ID с валидацией (красный/зелёный), автоверхний регистр, DLC 1–8, 8 полей Data HEX с автопереходом и Backspace.
- ✅ Блоки обёрнуты в `QGroupBox` с тонкими рамками, неактивный блок делается бледным через `setStyleSheet`.
- ✅ Внизу вкладки кнопки «Заводские настройки», «Сохранить в устройство», «Сохранить в файл».
- ✅ `ui/settings_window.py`: вкладка «Логика» добавлена в `QTabWidget`, поиск и `retranslate_ui` обновлены.
- ✅ `core/can_id_loader.py`: универсальный загрузчик, сканирует `library/can_id/`, читает `.dbc`, `.json`, `.csv` и `.md` (Markdown-таблицы + README-заголовки).
- ✅ `library/can_id/`: добавлены файлы из `iDoka/awesome-automotive-can-id` и `demo_data.json` с демо-записями (Toyota Camry, Kia Rio, BMW, Ford, VW, Honda, Nissan, Tesla, Mazda, Subaru).
- ✅ `ui/library_browser.py`: переработана в CAN ID библиотеку — дерево Марка → Модель → Год, таблица с колонками ID (HEX), DLC, Описание, Данные (пример), кнопка «Использовать в триггере», диалог добавления/редактирования ID.
- ✅ `ui/can_trigger_tab.py`: `create_trigger_from_packet` теперь устанавливает DLC из пакета.
- ✅ `build_win.spec`, `build_mac.spec`, `build_linux.spec`: добавлены `core.can_id_loader` и `ui.logic_tab` в `hiddenimports`, `library` уже включена в `datas`.
- ✅ `python3 -m py_compile` проходит; smoke-test: главное окно, окно настроек, вкладка «Логика», библиотека и триггеры запускаются без ошибок.

## 21. Подключение, прошивка и гибкая логика (14.07.2026)

- ✅ `ui/firmware_page.py`: добавлена панель подключения с выбором COM-порта, кнопкой «Подключиться/Отключиться» и статусом; отображаются тип устройства и серийный номер; порт FAKE (эмулятор) доступен.
- ✅ `ui/firmware_page.py`: добавлен `retranslate_ui()` для поддержки смены языка.
- ✅ `ui/flexible_logic_tab.py`: переработан интерфейс на три вертикальные колонки «Условие», «Действие», «Параметры» с неограниченными строками; ввод CAN-параметров (ID, маска, данные, ID ответа, данные ответа, маска замены, канал, задержка) выполняется прямо в строке.
- ✅ `ui/flexible_logic_tab.py`: добавлена кнопка «Из DBC» для быстрого заполнения условия; каждое правило имеет индикатор срабатываний и кнопку удаления.
- ✅ `ui/flexible_logic_tab.py`: `retranslate_ui()` обновляет строки строковых правил.
- ✅ `ui/can_monitor_tab.py`: увеличены минимальные высоты таблиц; добавлен выбор скорости CAN с ручным вводом; исправлен диалог фильтра и его работа с data_from/data_to.
- ✅ `ui/can_trigger_tab.py`: неактивные блоки «Ответ» и «Кэш» визуально делаются бледными; кастомные индикаторы заменены на `QCheckBox`.
- ✅ `ui/settings_window.py`: удалены вкладки «Логика» и «Графики».
- ✅ `ui/main_window.py` / `ui/settings_window.py`: навигация главное/настройки организована так, что одновременно открыто только одно окно.
- ✅ `ui/dark_theme.py`: исправлена светлая тема для верхней панели.
- ✅ `python3 -m py_compile` проходит; smoke-test: главное окно, окно настроек, прошивка, гибкая логика, мониторинг и триггеры запускаются без ошибок.

## 22. Исправления: прошивка, копирование/вставка, фильтр, мониторинг, поиск (14.07.2026)

- ✅ `ui/firmware_page.py`: убраны раздельные переключатели CAN1/CAN2; добавлен единый выбор типа устройства (2 CAN / 2 CAN FD / 2 CAN+).
- ✅ `ui/packet_clipboard.py`: создан единый модуль с кнопками «Копировать/Вставить» и парсером/форматером пакетов `ID=0x... DLC=N DATA=XX XX ...`; стиль кнопок использует `palette(button)`/`palette(midlight)` для корректного отображения в тёмной и светлой темах.
- ✅ `ui/can_trigger_tab.py`, `ui/can_monitor_tab.py`, `ui/can_gateway_tab.py`, `ui/library_browser.py`: добавлены кнопки копирования/вставки рядом с полями ID, DLC и Data.
- ✅ `ui/can_monitor_tab.py`: исправлена работа кнопки «Фильтр» (пустые `id_from`/`id_to` больше не приводят к падению `FilterDialog`); таблицы растянуты по вертикали; колонка «Пояснение» получила `Stretch` и увеличенную ширину.
- ✅ `ui/filter_dialog.py`: исправлена подстановка `id_from`/`id_to` при наличии пустых строк или `None`.
- ✅ `ui/settings_window.py`: полностью переработан поиск по разделам — `QCompleter` с `QStandardItemModel`, фильтрация по подстроке, подсветка и прокрутка к выбранному элементу; поддержка русского и английского языков.
- ✅ `models/translations.py`: добавлены `get_translation()` и `get_all_translations()` для двуязычного поиска.
- ✅ Все новые строки UI обёрнуты в `tr()`; проверены тёмная и светлая темы.
- ✅ `python3 -m py_compile` проходит; приложение запускается, главное окно, окно настроек, фильтр, поиск и копирование/вставка работают без ошибок.

## Итоговая оценка готовности

**100 %**

Приложение «Код Мастер» приведено к финальному состоянию: CAN 2.0, улучшенный UI/UX, прошивка STM32 с подключением, гибкая логика с тремя колонками, библиотека CAN ID, расширенный трейс и фильтр, индикаторы памяти, автоидентификация устройства. Код компилируется и проходит smoke-test.
